import { render, screen } from '@testing-library/react';
import Nav from './NavBackOffice';

describe('Nav component tests', () => {
  test('Renders correctly', () => {
    render(<Nav />);
  });
});
